Your entire reply must be that markdown and nothing else: no preamble,
no "I created the file", no tool narration. Start with a `# ` heading.
Also write the same bytes to `{{ document }}` at the workspace root.
